/** Shared backdrop visual tokens — single source of truth for modal overlays. */
export const BACKDROP_COLOR = 'rgba(0, 0, 30, 0.3)';
export const BACKDROP_BLUR_INTENSITY = 60;
export const BACKDROP_BLUR_TINT = 'dark' as const;
